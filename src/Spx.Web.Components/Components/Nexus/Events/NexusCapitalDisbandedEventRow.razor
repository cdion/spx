@using Spx.Nexus.Domain

@{
    var design = Context.DesignLookup[Event.DesignId];
}

<span>
    <span class="font-semibold @Context.PlayerNameClass(Event.PlayerId)">@Context.PlayerName(Event.PlayerId)</span>'s
    <NexusUnitBadge Category="design.Hull"
                     DesignName="@Event.DesignName"
                     Design="design"
                     Count="Event.Count" />
    at
    <NexusSystemBadge Coord="Event.System"
                       CurrentPlayer="@Context.CurrentPlayer"
                       Opponent="@Context.Opponent" />
    was disbanded (over supply limit)
</span>

@code {
    [Parameter, EditorRequired]
    public NexusCapitalDisbandedEvent Event { get; set; } = default!;

    [Parameter, EditorRequired]
    public EventRowContext Context { get; set; } = default!;
}
