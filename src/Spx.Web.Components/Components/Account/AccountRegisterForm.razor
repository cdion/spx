<FormPage Title="Create account"
          Description="Register with your email address. You will receive an email with a confirmation link to complete the registration.">
    @if (!string.IsNullOrWhiteSpace(Error))
    {
        <AlertBanner>@Error</AlertBanner>
    }

    <form method="post" action="/account/register" class="ui-flow">
        @FormTokens

        <FormField For="email" Label="Email">
            <input id="email" name="email" type="email" value="@Email" autocomplete="email" required class="ui-input" />
        </FormField>

        <FormField For="password" Label="Password">
            <input id="password" name="password" type="password" autocomplete="new-password" required
                   class="ui-input" />
        </FormField>

        <FormField For="confirmPassword" Label="Confirm password">
            <input id="confirmPassword" name="confirmPassword" type="password" autocomplete="new-password" required
                   class="ui-input" />
        </FormField>

        <p class="text-sm text-slate-400">
            Passwords must be at least 12 characters and include uppercase, lowercase, and a digit.
        </p>

        <button class="ui-button-primary" type="submit">Create account</button>
    </form>

    <p class="text-sm text-slate-300">
        Already registered?
        <a class="ui-inline-link" href="/login">Sign in</a>
    </p>
</FormPage>

@code {
    [Parameter] public string? Email { get; set; }
    [Parameter] public string? Error { get; set; }
    [Parameter] public RenderFragment? FormTokens { get; set; }
}
