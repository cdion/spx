// Domain types in Spx.Nexus.Domain are annotated directly with [GenerateSerializer] and [Id(N)].
// Surrogates are not needed for owned types — see spx-orleans-boundaries instructions.
namespace Spx.Contracts;
